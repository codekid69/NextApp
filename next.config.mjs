/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['media.istockphoto.com','images.unsplash.com','i.pinimg.com','example.com','imgs.search.brave.com']
    }
};

export default nextConfig;
